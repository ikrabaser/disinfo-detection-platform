from django.apps import AppConfig


class AIAnalysisConfig(AppConfig):
    default_auto_field = (
        "django.db.models.BigAutoField"
    )

    name = "ai_analysis"

    verbose_name = (
        "VERITAS AI Analysis"
    )
